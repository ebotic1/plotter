IDz promjene:
SettingsView.h (za restart aplikacije na macos) kompletan fajl

Simulate.cpp (uraditi merge):
Linija 422:

#ifdef USE_SCROLLVIEW_TABLE
		nameAndType = modelName;
		nameAndType += "-table";
		GlobalEvents::getMainWindowPtr()->getDataDrawer()->addData(nameAndType, outDataSet, DataDraw::Type::Table);
#endif


Results.cpp
Linija 3 (konstruktor):

Results::Results()
: _layout(1)
, _table(td::Ownership::Intern, gui::TableEdit::RowNumberVisibility::VisibleZeroBased)
{
    _layout << _table;
	setLayout(&_layout);
}

Linija 22:

void Results::show(dp::IDataSet* pDS)
{
    pDS->onRemoveAll([this](dp::IDataSet* pDS)
    {
        _table.clean();
    });

    dp::IDataSet* currDataSet = getDataSet();
    if (currDataSet == nullptr || currDataSet != pDS)
    {
        _table.init(pDS, 0);
        const auto& userInfo = pDS->getUserInfo();
        if (userInfo.lu8Val() == 0) //non-time domain, firs column is "eps" set it to scientific format
            _table.setColumnNumericFormat(0, td::FormatFloat::Scientific);
    }
    else //if (currDataSet == pDS)
    {
        _table.reload();
    }
    
}

Greške:
1. Ne rjesava case9 i IEEE9 (IDz)
2. Ne uzima u obzir RndGens i Stats (Emir)

Poboljsanja:
1. Kada se javi greška i ispiše se linija u logu, nemoguće naći gdje se ta linija nalazi. 
	- Dodati status bar koji će pokazivati trenutnu liniju i kolonu
	- Dodati opciju GoTo Line
	- Dodati mogućnost da se klikom na log sa linikom skoči na liniju u editoru

2. 
